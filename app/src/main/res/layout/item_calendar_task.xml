<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/root_calendar_item"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="8dp"
    android:padding="12dp"
    android:orientation="vertical"
    android:background="@drawable/bg_task_not_started">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <TextView
            android:id="@+id/text_title"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Task title"
            android:textSize="16sp"
            android:textStyle="bold"
            android:textColor="@color/black" />

        <androidx.appcompat.widget.AppCompatSpinner
            android:id="@+id/spinner_status_calendar"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:spinnerMode="dropdown"
            android:layout_marginStart="8dp"
            android:paddingLeft="0dp"
            android:paddingRight="10dp"
            android:background="@drawable/bg_status_spinner_grey"
            app:popupTheme="@style/SpinnerPopupTheme" />

        <ImageView
            android:id="@+id/img_edit_calendar"
            android:layout_width="28dp"
            android:layout_height="28dp"
            android:layout_marginStart="8dp"
            android:src="@drawable/ic_edit"
            android:contentDescription="Edit" />

        <ImageView
            android:id="@+id/img_delete_calendar"
            android:layout_width="28dp"
            android:layout_height="28dp"
            android:layout_marginStart="6dp"
            android:src="@drawable/ic_delete"
            android:contentDescription="Delete" />

    </LinearLayout>

    <TextView
        android:id="@+id/text_duration_calendar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="6dp"
        android:text=""
        android:textSize="14sp"
        android:textColor="@color/black" />

</LinearLayout>
